#ifndef __UTILS_H__
#define __UTILS_H__
/*@file A header file which acts as a utility for out bidding system */

/*
 * @brief When invoked initiates key exchange process using diffie-hellman key exchange protocol
 * 
 * @param int sock_fd:  socket file descriptor needed for communication
 * 
 * @return None
 */
 
void kxchg(int sock_fd);

/*
 * @brief 
 */
#endif
